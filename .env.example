# Used only by the server-side /api/analyze route. Leave empty for fallback mode.
OPENAI_API_KEY=

# Optional compatible endpoint/model overrides for the server-side analysis route.
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini
