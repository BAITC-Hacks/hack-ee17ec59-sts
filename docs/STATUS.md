# Статус проекта

_Последнее обновление: 2026-09-23_

## Единая рабочая папка
- Работа ведётся только во внешней папке `hack-ee17ec59-sts`, ветка `main` отслеживает `origin/main` на GitHub.
- В неё перенесена карта из локального коммита `55f22e9`: `app/map_view.py`, `data/geo/`, `scripts/fetch_districts.py`, `tests/test_map_view.py`.
- Вложенные репозитории целиком сохранены за пределами проекта в `../hack-backup-20260923-143347/nested/`. Полная предварительная копия находится в том же бэкапе; по разрешению пользователя исключены только две недоступные папки `.pytest_cache`.
- Прежние служебные файлы внешней папки сохранены в `../outer-untracked`; все их строки уже присутствуют в версиях из GitHub. `CLAUDE.md` и журналы участников сохранены.
- Работа Дамира доступна на GitHub в `codex/damir/simulation-engine`; по решению пользователя она остаётся отдельной от `main`.
- VS Code выполняет `fetch` каждые 180 секунд, пока проект открыт. Это обновляет сведения о ветках; для применения опубликованных коммитов используется `git pull --rebase`, для отправки своих — `git push`.
- Новое окружение Python находится в `.venv` внешней папки. Проверка: `.venv/Scripts/python.exe -m pytest -q --basetemp=.venv/pytest-tmp -o cache_dir=.venv/pytest-cache` — **13 passed in 7.56s**.

## Текущий репозиторий
- Целевой репозиторий: https://github.com/BAITC-Hacks/hack-ee17ec59-sts, ветка по умолчанию main.
- Каркас Akim-5 перенесён в корень репозитория: data/, engine/, ai/, api/, app/, tests/ и requirements.txt.
- Исходный README сохранён и дополнен; .gitignore объединён, AGENTS.md дополнен с сохранением общих правил.
- Проверка в отдельном окружении .venv: pytest -q — 7 passed in 0.14s.
- Рабочие ветки engine, ai-api, ui подготавливаются от коммита переноса.
- NorthernW0lf/akim-5 сохранён. Разработка движка, AI/API и UI остаётся следующей задачей команды.

- Сохранены новые документы коллег из d5c12cd: актуальный план приложения — Next.js; перенесённый Python-каркас содержит заглушки и отдельный контракт.

## История до переноса

# Статус проекта

_Последнее обновление: 2026-09-23_

## ✅ Работает
- В `akim-5/` создан отдельный каркас AI-симулятора управления городом: данные, интерфейс-заглушка и контракт.
- `load_data()` читает JSON-файлы; `pytest -q`: 7 passed.
- Зависимости установлены в `akim-5/.venv`; приватный репозиторий https://github.com/NorthernW0lf/akim-5 опубликован, ветки `main`, `engine`, `ai-api`, `ui` созданы от коммита `6f5bec6`.

## 🔨 В процессе
- Реализация движка, AI/API и UI — следующие задачи команды.

## ⛔ Блокеры
- Нет блокеров инициализации.
