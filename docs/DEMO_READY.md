# Демо: готовность на 23 сентября, 16:22

## Действия после возвращения

1. В `.env.local` заполнить только `OPENAI_API_KEY`, сохранить и перезапустить `npm run dev`. Ключ остаётся локальным, LLM_* используют тот же ключ.
2. Открыть http://127.0.0.1:3000/. Эталон уже восстановлен: Score 56.54, расходы 95.
3. «Получить совет» → проверить источник «AI-объяснение» → показать компромисс → «Применить совет» → «Отменить совет».
4. Говорить: «Помощник предлагает конкретное изменение, движок проверяет его стоимость и последствия. Мы видим выигрыш города и потери района до применения. Одной кнопкой обновляем решения; отмена возвращает исходный план».

На момент подготовки ключ пустой: работает «Объяснение по расчёту», live AI пока не проверен. Воспроизводимый сценарий и запрос: `AI_ADVICE_DEMO.md`, `examples/advice-scenario.json`.

## Подтверждено

- Последний полученный main: `3cdabb5`, включая Docker/standalone.
- 92 теста, lint и production build прошли повторно.
- Dev-сервер перезапущен на 127.0.0.1:3000 с `.env.local`.
- Браузер: 56.54 / 95 → совет 56.99 / 86 → Apply обновил решения → Undo восстановил исходный план.
- `.env.local` игнорируется Git. Подготовка не меняет формулу, каталог данных или чужие модули.

## Следующая интеграция карты

`origin/ui` на `bc1aea0`: SVG-карта, выбор района для активного решения, слои D после / изменение D, исправленные пропорции и схематичный Алматы с предупреждением. Это частичная E5; Leaflet/маркеры/критический слой ещё не опубликованы.

Нужно объединить `ScenarioBuilder.tsx`: сохранить `initialScenario`, `onSimulate(result, scenario)` и сброс результата через `onChange`, добавить выбор с карты и callback `onScenarioChange`. При Apply/Undo карта должна брать пересчитанный результат, иначе её локальный state после remount сбросится к baseline. `page.tsx` в UI-ветке не менялся от общего предка: обычный трёхсторонний merge сохраняет Apply/Undo. После объединения проверить выбор района, Apply, Undo и ручную правку.

`origin/ai-api` остаётся на `7244a36`: новых опубликованных A3/A4 нет. Docker smoke остаётся неподтверждённым: на машине нет Docker CLI. Финальная сдача и общий code review ещё впереди.
